import { useContext } from "react";
import CartWrapper from "~/components/organism/CartWrapper";
import { products } from "~/constants/products";
import AppContext from "~/store/AppContext";

const Cart = () => {
  return <CartWrapper />;
};

export default Cart;
